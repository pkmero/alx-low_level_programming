#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @num1: first integer
 * @num2: second integer
 * Return: returns the result of adding the two integers
 */
int add(int num1, int num2)
{
	int result;

	result = num1 + num2;
	return (result);
}
